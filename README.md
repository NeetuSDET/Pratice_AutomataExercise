# Pratice_AutomataExercise
2026 
git config --global user.name 'Neetu'
git config --global user.email 'neeturk2@gmail.com'

xcode-select -p
xcode-select --install
sudo rm -rf /Library/Developer/CommandLineTools
Password:
xcode-select --install
xcode-select -p

(base) rajnishatrismbp:2026_AutomationExerciseFixture rajnishkhatri$ /Library/Developer/CommandLineTools
git --version

git config --global user.name "Neetu Khatri"
git config --global user.email "neeturk2@gmail.com"
git config --global --list
softwareupdate --list
Software Update Tool
git config --global user.name "Neetu Khatri"
git config --global user.email "neeturk2@gmail.com"

git init 

(base) rajnishatrismbp:2026_AutomationExerciseFixture rajnishkhatri$ xcode-select --install
(base) rajnishatrismbp:2026_AutomationExerciseFixture rajnishkhatri$ 

git --version
git init  
git status
git add .
git commit -m "First commit"
git remote add origin git@github.com:NeetuSDET/Pratice_AutomataExercise.git
git branch -M main
it push -u origin main
git pull origin main --allow-unrelated-histories
git add .
git commit -m "intial commit"
git push -u origin main
it pull origin main --rebase
git push -u origin main
git push -u origin main --force
git push -u origin main
git status
git add .
git commit -m "second commit"
git push -u origin main

git status
git add .
git commit -m "Remove unused folder"
git push origin main
git status

git status

git add .
(base) rajnishatrismbp:2026_AutomationExerciseFixture rajnishkhatri$ git commit -m "today commit"
[main 2146f96] today commit
 2 files changed, 12 insertions(+), 39 deletions(-)
 delete mode 100644 .github/workflows/playwright.yml
(base) rajnishatrismbp:2026_AutomationExerciseFixture rajnishkhatri$ git push origin main





